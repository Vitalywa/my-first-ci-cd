name: Security Secrets Practice

on:
  on:
  push:
    branches: [ "main" ]

jobs:
  deploy-with-secret:
    runs-on: ubuntu-latest
    steps:
      - name: Dummy Step
        run: echo "Начинаем безопасный деплой..."

      # Шаг, где мы используем наш секрет
      - name: Connect to Production
        run: |
          echo "Имитируем подключение к серверу..."
          # Обращаемся к секрету через контекст ${{ secrets.ИМЯ }}
          echo "Используем пароль: ${{ secrets.MY_PROD_PASSWORD }}"
